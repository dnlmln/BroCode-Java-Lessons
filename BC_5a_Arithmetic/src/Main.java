public class Main {
    public static void main(String[] args) {

        // Arithmetic Operators

        int x = 10;
        int y = 2;
        int z;

        double a = 10;
        double b = 3;
        double c;

        //z = x + y;
        //z = x - y;
        //z = x * y;
        z = x / y;         // Displays the result without remainder because we're using integers.
        //z = x % y;      // Modulus displays the remainer only. Useful to determine if number is even.

        c = a / b;         // Displays the result without remainder because we're using integers.

        System.out.println(z);

        System.out.println(c);
    }
}
